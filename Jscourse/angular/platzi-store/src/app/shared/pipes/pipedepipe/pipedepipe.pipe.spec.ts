import { PipedepipePipe } from './pipedepipe.pipe';

describe('PipedepipePipe', () => {
  it('create an instance', () => {
    const pipe = new PipedepipePipe();
    expect(pipe).toBeTruthy();
  });
});
